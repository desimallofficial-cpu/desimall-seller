const DesiMallAvatar={initials(n){return String(n||'User').trim().split(/\s+/).slice(0,2).map(x=>x[0]||'').join('').toUpperCase()||'U';},apply(){let u={};try{u=JSON.parse(localStorage.getItem('desimall_user'))||{};}catch(_){}document.querySelectorAll('[data-user-avatar]').forEach(el=>{const src=u.Avatar||u.ProfileImage||'';el.innerHTML=src?`<img src="${src}" alt="Profile">`:`<span>${this.initials(u.Name)}</span>`;});}};document.addEventListener('DOMContentLoaded',()=>DesiMallAvatar.apply());